<div class='well'>
<%= form_for(@tag, url: { action: action }, html: {class:'form-horizontal'})  do |f| %>
  <fieldset>
    <legend >New Tag</legend>
    <div class='form-group'>
      <label for='inputEmail' class='col-lg-2 control-label'>Tag</label>
      <div class='col-lg-10'>
        <%= f.text_field :name, class: 'form-control', placeholder:'Tag' %>
      </div>
    </div>

    <div class='form-group'>
      <div class='col-lg-10 col-lg-offset-2'>
        <%= f.submit submit_label, class:'btn btn-primary' %>
      </div>
    </div>
  </fieldset>
<% end %>
<div>
